# contracts/ — interface seams (TRD §8)

Phase-0 contracts are the headers under `core/include/lattice/` and
`verification/include/lattice/` (Engine-API surface, solver, datasheet, chart, scene, oracle).
Markdown contract specs (engine-api, host-adapter, connectors, style-file) + their contract
tests are authored in Phase −1/1 (tasks T-B005..B010).
